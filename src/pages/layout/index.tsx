import { Header, Footer } from '@/widgets';
import Cards from '../../components/Cards';

const Layout = () => {
  return (
    <>
      <Header />
			<Cards />
      <Footer />
    </>
  );
};

export default Layout;
